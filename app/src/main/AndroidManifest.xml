<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="happ.es.happ">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">

        <activity
            android:name=".LogoActivity"
            android:label="Happ!"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PanelControlActivity"
            android:label="@string/title_activity_panel_control"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".MenuActivity" />

        <activity
            android:name=".ActosBodadActivity"
            android:label="@string/title_activity_actos_bondad"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".DeviceActivity" />

        <activity
            android:name=".QuestionaryActivity"
            android:label="@string/title_activity_questionary"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
    </application>

</manifest>